const Dashboard = () => {
    return (
        <div className="container mt-4">
            <h2>Panel de Control (Turnos)</h2>
            <p>Aquí mostraremos los turnos con sus estados.</p>
        </div>
    );
};

export default Dashboard;